import React from "react";

export default function PrivacyNotice() {
  return (
    <p className="text-xs text-slate-500">
      Privacy notice: Photos and videos may contain sensitive metadata such as GPS coordinates, device
      information, serial numbers, and timestamps. Files are analyzed in memory and deleted immediately
      after processing — nothing is stored or shared with third parties.
    </p>
  );
}
